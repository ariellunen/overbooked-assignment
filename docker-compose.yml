version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    env_file:
      - ./backend/.env
    depends_on:
      - mock-llm
  mock-llm:
    build: ./mock-llm
    ports:
      - "8080:8080"
